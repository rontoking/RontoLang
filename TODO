- Allow using if, while, for, postpone, ... without { } if it's just one line.
- 3D Camera, rename point to point2 and add point3, add matrix, polygon, sphere...
- 3D Shapes (minecraft blocks ;d)
- Finish RontoUI!!!
- Make it possible to use a variable inside a when/every/after/whenever... block even if it's not static (maybe make it final or copy it into the block's own local vars?)
- Make it possible to make expr type values with expr() and parse() being only a part of a statement (without a ;) so that they can be combined (maybe with + operator) into bigger expr values..